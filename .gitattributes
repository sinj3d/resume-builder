# Prompt golden files are byte-compared by both the Rust test suite and
# training/prompt_builder.py --selftest; force LF so checkouts on Windows
# don't break the parity assertions.
src-tauri/src/llm/golden/* text eol=lf
